<div id="hd-generator-container">
  <!-- Introductory Text (Centered) -->
  <div class="hd-header">
    <div class="hd-eyebrow">FREE & SELF-SERVE</div>
    <h2>Make a Sign for Hilario</h2>
    <p>Type your group, pick a color scheme and a format, and download a "[Group] for Hilario" graphic in the campaign's colors — for yard signs, social posts, or print.</p>
  </div>

  <div id="hd-generator-app">
    <!-- Sidebar Controls -->
    <div class="hd-controls">
      <label for="hd-group-input">Group Name:</label>
      <input type="text" id="hd-group-input" value="EDUCATORS" placeholder="e.g. LOGAN SQUARE" autocomplete="off">
      
      <label for="hd-theme-select">Color Scheme:</label>
      <select id="hd-theme-select">
        <option value="orange">Orange Background</option>
        <option value="blue">Blue Background</option>
      </select>

      <label for="hd-size-select">Graphic Size:</label>
      <select id="hd-size-select">
        <option value="square">Social Square (1080 x 1080)</option>
        <option value="story">Instagram Story (1080 x 1920)</option>
        <option value="landscape">Facebook / Web (1200 x 628)</option>
        <option value="yardsign">Yard Sign (2400 x 1800)</option>
      </select>
      
      <button id="hd-download-btn" type="button">Download PNG</button>
    </div>

    <!-- Live Preview Area -->
    <div class="hd-preview-wrapper" id="hd-preview-wrapper">
      
      <!-- THE EXPORT CANVAS -->
      <div id="hd-canvas" class="size-square theme-orange">
        
        <div class="hd-canvas-content">
          <!-- TOP & MIDDLE: Input Text and "FOR" Badge -->
          <div class="hd-text-group">
            <div id="hd-dynamic-group">EDUCATORS</div>
            <div class="hd-static-support">FOR</div>
          </div>

          <!-- BOTTOM: Campaign Logo -->
          <img class="hd-campaign-logo" 
               src="https://i.ibb.co/99H3jCgN/Hilario4-Board-Logo.png" 
               alt="Campaign Logo" 
               crossorigin="anonymous">
          
        </div>
      </div>
    </div>
  </div>
</div>

<style>
/* Main Container */
#hd-generator-container {
  background: #f4f4f5;
  padding: 25px;
  border-radius: 8px;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
  box-sizing: border-box;
  width: 100%;
}

/* Header Text Styles - CENTERED */
.hd-header {
  margin-bottom: 25px;
  padding-bottom: 20px;
  border-bottom: 2px solid #e5e7eb;
  text-align: center;
}

.hd-eyebrow {
  font-size: 0.85rem;
  font-weight: 800;
  letter-spacing: 1px;
  color: #f69e0b;
  text-transform: uppercase;
  margin-bottom: 5px;
}

.hd-header h2 {
  margin: 0 0 10px 0;
  font-size: 2rem;
  color: #111827;
  font-weight: 900;
  line-height: 1.1;
}

.hd-header p {
  margin: 0;
  color: #4b5563;
  font-size: 1.05rem;
  line-height: 1.5;
  max-width: 700px;
  margin-left: auto;
  margin-right: auto;
}

/* App Layout */
#hd-generator-app {
  display: flex;
  flex-direction: row;
  gap: 20px;
}

@media (max-width: 992px) {
  #hd-generator-app {
    flex-direction: column;
  }
}

/* Sidebar Controls */
.hd-controls {
  flex: 1;
  min-width: 250px;
  display: flex;
  flex-direction: column;
  gap: 12px; 
}

.hd-controls label {
  font-weight: 700;
  font-size: 0.9rem;
  color: #374151;
  margin-bottom: -8px;
  margin-top: 5px;
}

.hd-controls input, 
.hd-controls select {
  width: 100%;
  padding: 10px;
  border: 1px solid #d1d5db;
  border-radius: 6px;
  font-size: 1rem;
  box-sizing: border-box;
}

.hd-controls button {
  background: #1d4ed8; 
  color: #ffffff;
  border: none;
  padding: 14px;
  border-radius: 6px;
  font-weight: 800;
  font-size: 1.1rem;
  cursor: pointer;
  transition: background 0.2s;
  margin-top: 10px;
}

.hd-controls button:hover {
  background: #1e3a8a;
}

/* Preview Wrapper */
.hd-preview-wrapper {
  flex: 2;
  width: 100%;
  min-height: 55vh; 
  background: #e5e7eb;
  border-radius: 6px;
  position: relative;
  overflow: hidden; 
}

/* THE CANVAS & CSS VARIABLES */
#hd-canvas {
  --bg-color: #f69e0b;
  --accent-color: #1d4ed8;
  background: var(--bg-color); 
  position: absolute;
  left: 50%;
  top: 50%;
  transform-origin: center center;
  display: flex;
  flex-direction: column;
  box-sizing: border-box;
}

#hd-canvas.theme-orange {
  --bg-color: #f69e0b;
  --accent-color: #1d4ed8;
}

#hd-canvas.theme-blue {
  --bg-color: #1d4ed8;
  --accent-color: #f69e0b;
}

/* Exact Output Sizes */
#hd-canvas.size-square    { width: 1080px; height: 1080px; }
#hd-canvas.size-story     { width: 1080px; height: 1920px; }
#hd-canvas.size-landscape { width: 1200px; height: 628px; }
#hd-canvas.size-yardsign  { width: 2400px; height: 1800px; }

/* Canvas Internal Layout */
.hd-canvas-content {
  width: 100%;
  height: 100%;
  padding: 6%; 
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  box-sizing: border-box;
  color: #ffffff;
}

/* Default Logo Sizing (Global) */
.hd-campaign-logo {
  width: 100%; 
  max-height: 80%; 
  object-fit: contain;
  margin-top: auto; 
  position: relative;
  z-index: 1; 
}


/* =========================================
   1. GLOBAL & STORY SIZING (The Defaults) 
   ========================================= */
.hd-text-group {
  flex-grow: 0; 
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  width: 100%;
  margin-top: 25%;
  margin-bottom: -25%; 
  position: relative;
  z-index: 10; 
}
#hd-dynamic-group {
  font-size: 300px; 
  font-weight: 900;
  text-transform: uppercase;
  line-height: 1.1; 
  text-shadow: 6px 6px 0px var(--accent-color);
  word-wrap: break-word;
  overflow-wrap: break-word;
  max-width: 100%;
  text-align: center; 
}
#hd-canvas #hd-dynamic-group.hd-text-shrink { font-size: 225px; }
.hd-static-support {
  font-size: 135px;
  font-weight: 800;
  margin-top: 35px;
  background: var(--accent-color);
  color: #ffffff;
  padding: 20px 80px;
  border-radius: 200px;
}


/* =========================================
   2. SOCIAL SQUARE OVERRIDES
   ========================================= */
#hd-canvas.size-square .hd-text-group {
  margin-top: 15%; 
  margin-bottom: -15%; 
}
#hd-canvas.size-square #hd-dynamic-group { 
  font-size: 150px; 
  text-shadow: 3px 3px 0px var(--accent-color); 
}
#hd-canvas.size-square #hd-dynamic-group.hd-text-shrink { font-size: 112px; }
#hd-canvas.size-square .hd-static-support { 
  font-size: 67px; 
  margin-top: 15px; 
  padding: 10px 40px; 
}
#hd-canvas.size-square .hd-campaign-logo {
  margin-bottom: 8%; /* Logo moved up */
}


/* =========================================
   3. FACEBOOK / WEB (LANDSCAPE) OVERRIDES
   ========================================= */
#hd-canvas.size-landscape .hd-text-group {
  margin-top: 0%; /* Pushed to the top */
  margin-bottom: 0%; 
}
#hd-canvas.size-landscape #hd-dynamic-group { 
  font-size: 105px; 
  text-shadow: 3px 3px 0px var(--accent-color); 
}
#hd-canvas.size-landscape #hd-dynamic-group.hd-text-shrink { font-size: 82px; }
#hd-canvas.size-landscape .hd-static-support { 
  font-size: 52px; 
  margin-top: 10px; 
  padding: 10px 30px; 
}
#hd-canvas.size-landscape .hd-campaign-logo { 
  max-height: 85%; 
  margin-bottom: 5%; /* Logo moved up slightly */
}


/* =========================================
   4. YARD SIGN OVERRIDES
   ========================================= */
#hd-canvas.size-yardsign .hd-text-group {
  margin-top: 10%; 
  margin-bottom: -5%; 
}
#hd-canvas.size-yardsign #hd-dynamic-group { 
  font-size: 165px; 
  text-shadow: 5px 5px 0px var(--accent-color); 
}
#hd-canvas.size-yardsign #hd-dynamic-group.hd-text-shrink { font-size: 120px; }
#hd-canvas.size-yardsign .hd-static-support { 
  font-size: 71px; 
  margin-top: 25px; 
  padding: 15px 45px; 
}
#hd-canvas.size-yardsign .hd-campaign-logo { 
  max-height: 80%; 
  margin-bottom: 8%; /* Logo moved up */
}

</style>

<!-- Load html2canvas -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js"></script>

<script>
document.addEventListener("DOMContentLoaded", function() {
  const groupInput = document.getElementById("hd-group-input");
  const sizeSelect = document.getElementById("hd-size-select");
  const themeSelect = document.getElementById("hd-theme-select");
  const dynamicGroupText = document.getElementById("hd-dynamic-group");
  const canvasElement = document.getElementById("hd-canvas");
  const previewWrapper = document.getElementById("hd-preview-wrapper");
  const downloadBtn = document.getElementById("hd-download-btn");

  // Function to process Text and handle Format-Specific Logic
  function updateTextAndSize() {
    const textValue = groupInput.value || " ";
    dynamicGroupText.innerText = textValue;
    
    const currentSize = sizeSelect.value;
    // Set dynamic limit: 6 characters for story, 8 for everything else
    const charLimit = (currentSize === 'story') ? 6 : 8;

    // Trigger shrink class if text exceeds current limit
    if (textValue.trim().length > charLimit) {
      dynamicGroupText.classList.add("hd-text-shrink");
    } else {
      dynamicGroupText.classList.remove("hd-text-shrink");
    }
  }

  // Live Text Updates
  groupInput.addEventListener("input", updateTextAndSize);

  // Handle Theme Swapping
  themeSelect.addEventListener("change", function() {
    canvasElement.classList.remove("theme-orange", "theme-blue");
    canvasElement.classList.add("theme-" + this.value);
  });

  // Visual Scaling Engine
  function updateScale() {
    canvasElement.style.transform = "translate(-50%, -50%) scale(1)";
    
    const wrapperWidth = previewWrapper.clientWidth;
    const wrapperHeight = previewWrapper.clientHeight;
    const canvasWidth = canvasElement.offsetWidth;
    const canvasHeight = canvasElement.offsetHeight;

    const scaleX = (wrapperWidth * 0.92) / canvasWidth;
    const scaleY = (wrapperHeight * 0.92) / canvasHeight;
    const scale = Math.min(scaleX, scaleY);

    canvasElement.style.transform = `translate(-50%, -50%) scale(${scale})`;
  }

  // Handle Size Swapping
  sizeSelect.addEventListener("change", function() {
    canvasElement.classList.remove("size-square", "size-story", "size-landscape", "size-yardsign");
    canvasElement.classList.add("size-" + this.value);
    updateTextAndSize(); // Recalculate text limits when format switches
    updateScale();
  });

  // Initialize size and text on load
  window.addEventListener("resize", updateScale);
  updateTextAndSize(); 
  setTimeout(updateScale, 150);

  // Download Function
  downloadBtn.addEventListener("click", function() {
    const originalText = downloadBtn.innerText;
    downloadBtn.innerText = "Generating...";
    downloadBtn.disabled = true;

    const currentTransform = canvasElement.style.transform;
    canvasElement.style.transform = "translate(-50%, -50%) scale(1)";

    html2canvas(canvasElement, {
      scale: 1, 
      useCORS: true,
      backgroundColor: null,
      logging: false
    }).then(canvas => {
      const link = document.createElement('a');
      const sizeLabel = sizeSelect.value;
      const themeLabel = themeSelect.value;
      const groupLabel = groupInput.value.replace(/[^a-z0-9]/gi, '_').toLowerCase() || 'graphic';
      
      link.download = `Hilario_${groupLabel}_${themeLabel}_${sizeLabel}.png`;
      link.href = canvas.toDataURL('image/png');
      link.click();
      
      canvasElement.style.transform = currentTransform;
      downloadBtn.innerText = originalText;
      downloadBtn.disabled = false;
    }).catch(err => {
      console.error("Export error:", err);
      alert("There was an issue generating the image. Make sure your asset URL allows cross-origin requests.");
      canvasElement.style.transform = currentTransform;
      downloadBtn.innerText = originalText;
      downloadBtn.disabled = false;
    });
  });
});
</script>
